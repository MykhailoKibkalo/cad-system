import styled from "@emotion/styled";

export const Panel = styled.div`
  position: absolute;
  top: 36px;
  right: 28px;
  width: 340px;
  bottom: 36px;
  background: #FFF;
  padding: 16px;
  box-shadow: -4px 0 4px rgba(0, 0, 0, 0.1);
  overflow-y: auto;
  border-radius: 16px;
  z-index: 1100;
`;
